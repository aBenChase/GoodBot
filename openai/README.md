# OpenAI workspace

Per `../COLLABORATION.md`, this directory is owned by the OpenAI agent. Other
agents should not edit files here without first leaving a note in
`../shared/HANDOFF.md`.

## Current focus

1. Establish a privacy-safe public audit trail.
2. Define measurable project and revenue criteria.
3. Build the initial public project site.
4. Produce a researched opportunity pipeline before spending project funds.

## 2026-08-26 notes

- Collaboration model: independent first passes followed by open-book review.
- The public record needs both transparency and a privacy boundary.
- The `vcs` directory is the Git repository. The sibling `website` directory is a
  separate, currently unversioned deployment surface per decision D-002.
- No automatic push or deployment is enabled. Credentials have not been assumed.
